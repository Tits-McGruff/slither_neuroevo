# Rust-authoritative runtime factual implementation log

This is the short factual log required by
`docs/todo/rust-authoritative-runtime-plan.md`. It records approvals, commits,
evidence artifacts, measured results, and deviations. It does not restate the
plan or turn an unfinished stage into a completed one.

## Approved plan

- Owner approval received: 2026-07-29.
- Exact approved revision: `2026-07-29-draft-4`.
- Git commit containing the exact reviewed plan:
  `7971ed2ddbda86891c77def31d980aedf96b4236`.
- Plan blob in that commit:
  `bad8dafd8304fd5f81c0f37eb812fba8f2adb2da`.
- Approval scope: the architecture, migration path, persistence/archive
  design, 22 recorded owner decisions, corrections, tests, evidence gates, and
  acceptance requirements in Draft 4.
- Earlier drafts `2026-07-23-draft-1`, `2026-07-28-draft-2`, and
  `2026-07-28-draft-3` were superseded and were not approved.

## Implementation entries

| Date | Stage | Commit | Evidence/result | Known issue or deviation |
|---|---|---|---|---|
| 2026-07-29 | Approval and Stage 1 start | Plan: `7971ed2ddbda86891c77def31d980aedf96b4236` | Owner explicitly approved `2026-07-29-draft-4`; working tree began Stage 1 from implementation base `46c2f634c4cf48b1c1d30b7b55e3373495773d4e`. | None. No earlier draft is approved; no Stage 1 exit gate is yet claimed. |
| 2026-07-29 | Stage 1 architecture record and Git-history evidence | `c489c7e` | Corrected active instructions/README, superseded ADR 0001 and the false recovery-plan authority, added ADR 0002, and retained exact Git commit/blob/command evidence in `docs/todo/evidence/2026-07-29-stage1-git-history.md`. The false-document, historical 10/20-tick controller, and commit `8330065` claims all reproduced from Git and remain distinct from current-source proof. | Runtime repairs and the Stage 1 exit gate remain open. |
| 2026-07-29 | Stage 1 body sensing and collision-index admission | `ac43cde2f0c4913c0b416f41f60077b33f019caf` | Replaced the obsolete body-sensor adapter with the production callback grid contract; neural, baseline, and external observation paths now pass a real-grid body fixture. Collision-grid rebuilds preflight and grow capacity, reject unsafe admission instead of truncating, clear stale references, and expose load/capacity/peak/rebuild/growth/admission/fault diagnostics through stats and health. A 200,001-segment regression retains and queries every entry. Focused tests: 43 passed; world/core/server integration: 27 passed; TypeScript and changed-file ESLint passed. | The 32-Mi-entry ceiling is a temporary TypeScript-reference implementation limit, not a Rust capacity decision. Stage 1 controller, scheduler, browser-input, and reliable-message repairs remain open. |
| 2026-07-29 | Stage 1 controller, catch-up, browser input and lifecycle priority | `cf0559722d24a9fbc907e42098c22d8aa87a0d5b` | Added configurable 500-ms wall-time input hold and 30-second disconnect grace; neutral ownership during grace; token/legacy reconnect of the same snake; boundary-only expiry; latest-value browser actions; independent screen-space pointer sending; Node service between interactive catch-up steps; startup-clock reset; one reliable JSON queue ahead of one replaceable frame; and outbound diagnostics. Real socket reclaim, suppressed-sensor browser/server control, write-failure and scheduler tests pass. | The browser exposes 30 Hz and 60 Hz candidates with a temporary 60 Hz default; Stage 2 must measure both before the initial configurable cadence is selected. Reset/New Run/import epoch invalidation remains in its later approved stage. |
| 2026-07-29 | Stage 1 known-defect correction fixtures | `729cbd85a8c332bf56b76b5180cfb0d71f7648c3` | Added explicit expected-failure correction fixtures for array-order collision bias (`COLL-002`), unsafe complete-body spawn admission (`SPAWN-001`), external-join world/evolution RNG contamination (`RNG-001`), and Float32 frame-ID aliasing (`FRAME-002`). Expanded the saturated-frame test to drain assignment, reclaim, sensors/control traffic and errors before the newest frame. | These four fixtures still fail against the temporary TypeScript behavior by design; they are requirements for the named Rust stages, not passing claims or golden masters. |
| 2026-07-29 | Stage 1 real browser event transmission | `2205ff66b5b758ba42cd8af50329da8e8bb4886b` | The imported browser entry point receives one sensor pose, then actual canvas move/press/release events continue to emit fresh steering and boost-off actions without another sensor or display frame. Together with the server/world integration fixture, the newest command reaches the controller registry and next eligible fixed step. Current full suite: 68 files/340 tests passed (including four declared expected-failure correction fixtures); TypeScript, changed-file ESLint and the production browser build passed. | The Stage 1 correctness/reference exit is met. The 30/60-Hz measurement checkbox remains open and carries into Stage 2; no Rust-engine or performance exit is claimed. |
| 2026-07-29 | Stage 2 reproducible source/behavior/runtime baseline | `9a2e1fe`, `8b2b216`, `6949ae3` | Added named P0–P4 fixtures and clean-source runners for current database, codec, behavior, graph, direct runtime and canonical Node-worker paths. Retained the TypeScript source-to-fixture map, fixed-seed cross-cutting behavior artifact, current kill-credit examples, development-machine P0–P4 runtime artifacts, environment identity and derived native crossing counts. | These are Windows Ryzen 7 5800X development results. Target Debian output, real LAN/browser/trainer runs, sustained dense/soak fixtures and Stage 2 exit remain open. |
| 2026-07-29 | Stage 2 codec and narrow SQLite evidence | `341c191`, `b69b2c5`, `5b09545`; corrected runner: `1e36cd7` | Measured only representative compressed byte-volume SQLite operations rather than building a competing checkpoint system. Retained raw/decimal-JSON/gzip/plain-Zstandard comparison/byte-shuffled-Zstandard/per-genome results for fresh and evolved-like P0–P3 weight fixtures with bit-exact decode. Codec evidence version 2 enforces the approved archive-v1 choice between raw packed and shuffled Zstandard only. | The initial runner incorrectly allowed plain Zstandard to win its labelled adaptive choice for evolved P0. That label/selection was corrected before production archive implementation or retention measurement. The data still covers numeric weight payloads, not a complete managed checkpoint container; target-VM and full-checkpoint measurements remain open. |
| 2026-07-29 | Stage 2 database inventory and accidental growth observation | `9669b6b`, `66a70ac` | Bounded the read-only inventory query, reproduced the retained 5,921,520 weight-byte/6,041,600 database-byte case, and retained an accidental disposable P1 current-server run that accumulated 128 checkpoints and grew to 2,155,540,480 bytes over 8.906 hours. Exact raw logs and database inventory identify the source and limitations. | The accidental run is not the controlled P8 soak, target-VM evidence, or selected managed-file design. No owner database was written. |
| 2026-07-29 | Stage 2 current browser persistence failure | `ffb5148`, `ec1cc70`, `f25bf8f` | A finite fixture host and the in-app browser reproduced the current P1 export/import path. A population with 16,149,600 packed weight bytes produced 81,293,145 bytes of decimal JSON; export peaked at 256,485,515 combined reported heap/backing bytes. Import failed at the generic request limit after 53,018,624 uploaded bytes. Raw traces, source/build identities and exact limitations are retained. | The initial long browser-tool pause was owner-approval wait for monitoring the loopback address/port, not application latency; it is excluded from every reported timing. Import parse-time peak was unobservable while the browser action was busy and is not claimed. |
| 2026-07-29 | Stage 2 current real-server external-control baseline | Runner: `24dca58`; evidence: `98b4ea4` | Retained clean-source P0/P1 30/60-Hz runs through the real current server with simultaneous Protocol 2 periodic UI control, observation-driven bot control and complete frame-v1 viewing. P0 held about 1x; P1 managed only 0.626–0.637x, dropped 6.9–7.03 simulated seconds, and delivered five frames in each fifteen-second window. | Loopback Node clients are not the real browser renderer, owner trainer, another LAN device or Debian VM. The runs do not yet select 30 versus 60 Hz. Tool permission waits are outside runner-monotonic measurement. |
| 2026-07-29 | Stage 2 accelerated retention and growth fixture | Runner/hardening: `1e36cd7`, `e489b3c`; evidence: `6205144` | Retained `retention-p8-p0-480.json` with SHA-256 `72fd0d7fbdb28a105305970210e93d01c78a7869952556e049e26cf4e40347c5`. The size-matched P0 fixture represents 480 generation checkpoints and preserves all 480 compact history and Hall-of-Fame metadata rows while pruning to 22 automatic checkpoints (50,079,788 bytes) and 50 Hall-of-Fame weight files (2,480,183 bytes). All file/metadata/current-pointer accounting assertions passed. Indexed P1–P3 projections show the 4-GiB cap, rather than count alone, controls P3. | The payload files are not checkpoint-v3 USTAR archives, are not importable or fsynced, and do not prove publication durability, restore, archive latency or target-VM performance. P1–P3 are arithmetic from retained codec artifacts, not materialized retention runs; pinned and exported files remain outside the automatic cap. |
| 2026-07-29 | Stage 2 managed-checkpoint write-validation comparison | Runner: `ac905db`; evidence: `6c3ec68` | Retained clean-source P0/P2 artifacts with SHA-256 `efa57db87552c61452ebb48240d49c562c50b33ecb254a4d4a5a2cfd40bb7e96` and `59bbd3013ea85bb65b0894b24633305f2293b115ede99f9047ad1d3f2055caed`. Seven trials of each single-pass/checksum/light-scan/full-decode choice and a strict corruption matrix select one-pass hash/count, codec/container completion, file fsync, final-length check, atomic rename and Debian directory fsync for ordinary automatic checkpoints. Frame checksum and a payload-blind second scan remain off; startup/import/restore validates strictly, and future manual export/pin creation fully decodes before success. Focused format tests passed 8/8; TypeScript, targeted ESLint and diff checks passed. | This is a disposable Node/Windows comparison, not production Rust, restore, SQLite or Debian durability evidence. P2 single-pass p95 was 587.623 ms on the 5800X and remains unproved on the Ryzen 7 2700. Windows directory fsync returned `EPERM`. The full JavaScript suite passed 361 tests but one unrelated native-worker determinism test exceeded its timeout under suite contention; that file then passed 6/6 alone. |
| 2026-07-29 | Stage 2 persistence-format compatibility inventory | `fbb6200` | Retained a current-source and Git-history matrix for format-2 SQLite rows, historical all-parent JSON and combined-gzip rows, current/older population JSON, graph-spec JSON and historical browser-local population state. History proves the compressed format introduced by `05e0b53` was a SQLite BLOB; no tracked standalone compressed population exporter or save object was found. Existing generated current/legacy persistence suites passed 27/27. | The search does not prove that external or renamed saves do not exist. Only the workspace `data/slither.db` is inventoried; the Debian VM, Unraid storage, other disks, backups and browser profiles remain open. No compatibility reader may be narrowed or retired until those owner files are identified and copied or inspected read-only. |
| 2026-07-29 | Stage 2 dense-world load-collapse timeline | Runner: `821b95e`; evidence: `6bae744` | Retained clean-source `runtime-p4-native-dense-600.json`, SHA-256 `fb8bdeaed0a8864dde8c084e847f5be081ab48c24fcda5137737f23088b63ba6`. The fixture began with 310 live snakes, 217,000 body points and exactly 216,690 indexed segments. The first completed step took 266.05 ms and reduced the load to 52 snakes/37,329 segments; after 600 steps it had eight snakes/9,905 segments. Exact admission, zero-out-of-bounds, fault-free and complete-step assertions passed. | This is an honest Windows load-collapse curve, not sustained P4, browser/LAN, or target-VM capacity. Its 1.54x average is dominated by the much smaller world after immediate deaths; the sustained-density and Ryzen 7 2700 gates remain open. |
| 2026-07-29 | Stage 2 legacy SQLite BLOB/TEXT slice probe | Runner: `8f25214`; evidence: `6bae744` | Retained clean-source `sqlite-legacy-slice-p0.json`, SHA-256 `a55a71d808d4371ad5d69347d79692d34bf65358ec78dccb024dd08e7c80d7cc`. On `better-sqlite3` 11.10.0/SQLite 3.49.2, 39 queries apiece reconstructed 2,527,124-byte BLOB and multibyte UTF-8 TEXT fixtures exactly while returning no JavaScript Buffer larger than 65,536 bytes. Focused tests, TypeScript, targeted ESLint, diff checks and an independent evidence review passed. | Boundary-only memory samples cannot observe transient SQLite/native allocation. The artifact therefore records `boundedNativeAllocationProved: false` and `productionQueryPathAuthorized: false`; Stage 6B needs a reviewed native incremental-BLOB path or a separately reviewed strict legacy ceiling. No owner data was read. |
| 2026-07-29 | Stage 2 P6 committed-step measurement hardening | `d60cd57` | The current-server runner now measures a minimum of 1,800 observed authoritative committed steps after an absolute tick-300 warm-up, records the actual start/end ticks and load, bounds health requests by the remaining run deadline, and obtains world-load metadata cached at a committed/post-pump boundary. Focused tests passed 11/11; TypeScript, targeted ESLint and diff checks passed. | The diagnostics include an O(snakes) scan. Separate runs can differ in RNG/action/viewer/polling timing and live load, so viewer-off/on results are not paired causal comparisons. Client sends do not prove server acceptance or fixed-step application. The full JavaScript suite passed 377 tests and had four timeout-only failures under 75-file contention; each affected file then passed independently, 22/22 in total. |
| 2026-07-29 | Stage 2 accelerated current-server P6 characterization | Runner: `d60cd57`; evidence: `074cab3` | Retained 20 clean-source P0/P2 runs at requested 1/2/4/8/12x with the synthetic loopback viewer off/on, plus one extended generation-boundary run, under `docs/todo/evidence/stage2/windows-5800x/p6/`. `SHA256SUMS.txt` has SHA-256 `92b07e08727019e2832433c11c84d4d005f435c49ca769f527432af4ed3358dc`. The short 1x windows had zero dropped simulated-time debt and achieved 1.000x for P0 and 1.016–1.021x for P2. Every 4x-or-higher short-window run accumulated large dropped debt and achieved only 1.568–2.508x. P0 2x achieved 2.088–2.096x but began with 1.269–1.683 seconds of pending debt, so it is not evidence of clean sustained 2x operation. The extended P0 12x run observed 18,004 steps, advanced generation 1 to 2, advanced current SQLite snapshot ID 1 to 2, achieved 3.674x, and dropped 683.133 simulated seconds. | These are independent Windows Ryzen 7 5800X current-server runs with materially changing snake/body load. The viewer is a same-process Node spectator counting complete frame-v1 messages, not a browser renderer, LAN client, latency probe or causal viewer-overhead measurement. The extended result proves only that one current interval-one SQLite generation checkpoint completed and status advanced; it does not measure checkpoint latency, crash durability, restore, managed files, checkpoint-v3, fsync or power loss. P0/P1/P2 targets, sustained headroom and Stage 2 exit remain unproved. |
| 2026-07-29 | Stage 2 compact-history SQLite overhead | Evidence: `074cab3` | Retained `history-sqlite-overhead-p8-480.json`, SHA-256 `963cbd053c61cc4bcf9ff0264d421493bc15682487e6ac622ab4eeaf3da5b2cb`, from clean source `d60cd57`. Four hundred eighty fixed 56-byte/eight-field records occupy 26,880 logical bytes in a 77,824-byte SQLite database with 19 pages and no freelist pages. Separate `synchronous=FULL` transactions measured append p50 1.971 ms, p95 2.176 ms, p99 2.903 ms and max 6.132 ms; the peak WAL was 4,120,032 bytes, and a passive checkpoint processed all 36 frames in 2.559 ms without truncating the WAL. | This is a disposable fixed-record SQLite test, not the production schema, checkpoint/archive/managed-file path, restore/durability proof, overnight run or target Debian VM. It measures transaction completion, not power-loss durability. |
| 2026-08-01 | Stage 2 all-parent legacy compatibility fixture | `399dbc1` | Added `server/test/fixtures/legacy-all-parent-v0.json`, SHA-256 `d37dd3b910806e962e29df1630bbe798cb17a1f20119fcbce5a4a49f109bea49`, and a focused read-only compatibility test. It proves a format-null row with no combined BLOB or child rows loads through the all-parent JSON reader, reconstructs the represented JSON numbers to exact Float32 bytes, emits its bounded-compatibility warning, and leaves the source row/file and child tables unchanged. Persistence/recovery tests passed 28/28; the independent fixture review passed. | This small synthetic fixture exercises the current materialized reader only. It is not an owner save, a large/bounded streaming import, HTTP transfer, Rust conversion, or permission to retire any reader. |
| 2026-08-01 | Stage 2 current-reference P7 soak | Harness/evidence: `399dbc1`; measured parent `074cab3` with `source.dirty = true` | Retained `p7-current-server-p0-30hz-30min.json`, SHA-256 `6d6482e02fd94dd62b1c80b739065a2dfa394648a585a84555d28dc4e612ef06`. Over 1,800,011 ms it completed at the final sample with 0.999997x scheduler speed and zero dropped simulated-time debt, advanced generation 1→9, passed nine same-snake/token-rotating reclaims and five explicit saves, and ended with fourteen snapshots/770 genome rows. Peak sampled RSS was 286,683,136 bytes; post-warmup slope was 349,724 bytes/minute and final RSS was 17,338,368 bytes below the warm-window median. Focused P7 tests passed 11/11, persistence/recovery tests passed 28/28, and TypeScript/targeted ESLint/diff checks passed. | Commit `399dbc1` preserves the exact measured runner whose SHA is `3ec3e957695c293857d5c48b4d6fb6c26e475c665e3ab0bc5552a3f20ec3b461`; the artifact's dirty-source limitation remains explicit rather than being rewritten as a clean run. This is the current TypeScript server with same-process Windows loopback clients, not a browser/LAN, owner trainer, Debian/Ryzen 2700, Rust, managed-retention, accepted/applied-action, fixed-step-percentile, or frame-byte result. The earlier loopback-monitoring approval wait occurred before runner readiness and is excluded. Intentional socket replacement emitted the known closed-socket reliable-send race, so no zero-lifecycle-failure claim is made. The full-suite wrapper could not start under the restricted filesystem and its escalation was unavailable at the usage ceiling; that is not recorded as a passing or failing suite. |
| 2026-08-01 | Stage 2 actual local-browser cadence | `c364918` | From clean source `5b9b493`, the built Chrome 150 browser UI connected, rendered P0, joined and reclaimed a player, and emitted real WebSocket actions at 58.50/s for the 60-Hz candidate and 29.56/s for the 30-Hz candidate. Controlled drags sent boost-on followed by boost-off after 17.189/33.786 ms. The bounded display windows delivered 23.28/16.79 frames/s with p95 51.90/65.45 ms. The summary artifact `browser-live-p0-30-60hz.json` has SHA-256 `c6d33fc7c0d1f9ce8d72d435e76d6f5d6bf96d17e88e6b57420d81b555000187`; the production browser build and JSON/diff validation passed. | A synthetic sub-33-ms 30-Hz tap was coalesced to boost-off without transmitting boost-on. The display windows occurred at different world states and are not a cadence comparison; both miss the final 25-fps/40-ms gate. This is same-machine loopback, not trusted LAN, owner trainer, target VM or accepted/applied-step latency. Raw CDP events and the screenshot were not retained. Host/tool/permission waits are outside the bounded CDP timestamps. No 30/60-Hz choice is finalized. |
| 2026-08-02 | Stage 2 owner data, graph and trainer audit | Runners/docs: `e79ebd5`, `0c7cd52`; evidence: `2856386` | Read-only inventory of `/opt/apps/slither_neuroevo/` found one pre-format 11,726,848-byte owner database with no population snapshots and two decimal-JSON Hall-of-Fame genomes; it permits compatible extraction but not exact resume. No standalone save/archive was found in that scoped server directory. The retained Windows/Debian graph outputs from the same identified input SHA matched exactly, correcting the prior claimed ordering difference while preserving the structural `localeCompare` risk. The external PyRL trainer at `5932884fa35065f19eabb1abb05b39ab3a3c112f` speaks Protocol 1 while the current server requires Protocol 2. | The exact graph input database bytes are not retained. Client download/backup locations were not searched. Trainer compatibility requires coordinated work and remains open. The audit refreshed the deployed checkout's Git index metadata, created empty-WAL/32-KiB-SHM SQLite sidecars, and wrote npm diagnostic logs; the main owner database and tracked source/ref state were unchanged. These side effects remain disclosed and were not deleted without owner direction. |
| 2026-08-02 | Stage 2 Oxygen current-runtime and real-server baselines | Runners: `93e22c1`, `284ed44`; evidence: `2856386` | On the Ryzen 7 2700 VM, current native-serial direct execution reached 1.220x P0, 0.228x P1, 0.672x P2 and 0.126x P3; P2 with four Node workers reached 0.906x with a poor tail. P4 collapsed from its dense initial state and later accumulated 829,938 rejected out-of-bounds midpoint insertions. Real-server synthetic P5 retained P0/P1/P2 at 30/60-Hz candidates; P1 achieved only about 0.264–0.268x and delivered two display frames in each fifteen-second run. | These are current TypeScript-reference results, not future Rust performance. Synthetic sends do not prove server acceptance/application. Sustained P4, target P6/P7, real-browser P1/P2, trainer, and final cadence selection remain open. |
| 2026-08-02 | Stage 2 Oxygen codec and managed-checkpoint prototype | Runners: `cd9ab28`, `4cd1c00`; evidence: `2856386` | Retained evolved P0/P2 packed-weight codec and disposable managed-checkpoint results with target provenance. Decimal JSON was about 5.01x raw packed weights. The selected raw-or-shuffled-Zstandard encoding decoded bit-exactly. Single-pass checkpoint publication p95 was 44.69 ms for P0 and 712.91 ms for P2; the prototype exercised file fsync, rename, and parent-directory fsync on Debian. | These are Node prototypes, not production Rust, state restoration, power-loss durability, retention pruning, or a complete persistence acceptance result. P2 production latency remains measurement-gated. |
| 2026-08-02 | Stage 2 real desktop-browser LAN P0 summary | Source: `284ed44`; evidence: `2856386` | A real Chromium 150 desktop browser connected to Oxygen over the trusted LAN, joined/reclaimed a P0 snake, and emitted 396 actions spanning 395 inter-send intervals over 6.776742 seconds (58.288 intervals/s); a captured boost-on was followed by boost-off after 18.207 ms. The same accidental long-duration current-reference process ended after 32,876.982 wall seconds at 0.999778x and generation 142. Its stable disposable database was 41,385,984 bytes, dominated by 38,188,674 bytes of 142 repeated decimal-JSON Hall-of-Fame genomes while containing only one population checkpoint. | This is a manually assembled summary: raw CDP/health/process/SQLite transcripts were not retained. It does not prove server acceptance or step application, sensor suppression, a controlled soak, laptop/P1/P2 behavior, trainer compatibility, or Rust performance. |
| 2026-08-02 | Stage 1 delivered-sensor and token-boundary correction | `60ce843` | A controller observation now advances `points_delta_norm` only after the matching outbound sensor result is accepted. Failed assignment/reclaim/reassignment delivery no longer installs a token or claims a replacement snake; successful reclaim rotates the token only after both reliable results are accepted. Profiler timing stops before serialization and enqueue. Focused controller/world/server suites passed 55/55, with TypeScript, targeted ESLint and independent review passing. | Synchronous queue acceptance is the current reference boundary. A later asynchronous transport failure is not represented as successful delivery and remains part of the Rust lifecycle/epoch integration work. |
| 2026-08-02 | Stage 2 reproducible graph fixtures | `9a0cc5e`, `b500175` | Added exact LF-stable replay fixtures for the owner-derived default graph and an adversarial locale-sensitive implicit-Concat graph. Clean source `b500175` produced matching Windows/Oxygen layouts under Node 24.12.0, ICU 77.1 and locale `en-AU`; no cross-platform difference reproduced. The default fixture is 802 bytes/SHA-256 `3588820b9787204bec0f80df9d5b9a1faa368ac1d26e89b86c35604a3999894e`; the adversarial fixture is 1,833 bytes/SHA-256 `22b85250d08de5d15d6d46989e6fb64f7a98b1c3470ff40707d4bcfa7395b922`. Raw artifact SHA-256 values are Windows default `6c2b4de425d5e7abbdb909df821ef1e180ddabf34f19a54c87f6afa459b71ad4`, Windows adversarial `232d9c09c890ec0b43495c1076b6ecaef0c315d28e9c3c30eab3f62b5536c7de`, Oxygen default `cae75694242f4f7763289a866e8531e60f9916300b4d678f67b2b28dee1c9db8`, and Oxygen adversarial `e210c59e4916b0b7f1675b02b4f2a9f47903ef4896995d7f4f49b7e05653f5ed`. | Equal output for this environment pair does not remove the structural `localeCompare` compatibility risk. The scoped owner database's second large-MLP Hall-of-Fame architecture is retained only as an architecture key, so the broad real-graph inventory remains open. |
| 2026-08-08 | Stage 2 expanded Oxygen P6/P7/checkpoint/graph evidence | `32c6af9` | Added 29 root JSON artifacts plus the independently indexed 30-artifact P6 matrix. Fresh P0/P2 managed-checkpoint prototypes, clean graph-v2 pairs, a 30-run current-server P6 matrix, and a clean 30-minute current-server P7 P0 soak are retained with exact manifests and LF-stable evidence attributes. The P7 artifact SHA-256 is `e38536f68ca36451d0ea84ee1f89b686114e65d938ae1fac9d34de21c674fce7`; the root manifest SHA-256 is `fb11d501231634201cf53de18592a53a8ec49091c91af6aa0d3acd482cc71f5a`. | Current TypeScript/native-serial and synthetic-loopback evidence is not Rust, real browser/LAN/trainer, accepted/applied-action, managed-retention, restore/recovery or full P7 acceptance. |
| 2026-08-08 | Stage 2 memory-peak telemetry correction | `c30ea33` | The P5/P6 and P7 runners now include initial, periodic, retained scalar and final observations in their existing sampled-memory maxima as applicable. P5/P6 adds an explicit 50-ms cadence/caveat; P7 names its 250-ms timer plus scalar/endpoints. Real short P6/P7 integration coverage and helper regression passed 14/14; TypeScript, targeted ESLint and independent review passed. | Sampling still cannot observe spikes shorter than its cadence, and retained old artifacts are not rewritten. |
| 2026-08-08 | Stage 3 Rust engine foundation | `0f4fe9780aee437c5d5e4cf2ba96f10716bd2c0d` | Added Rust-owned graph/RNG/state admission, distinct heterogeneous population and recurrent-state ownership, checked memory/capacity contracts, bounded command/output queues, one stoppable coordinator-thread runtime, caught-panic faulting, and independently reproduced source/build identities. The release profile now unwinds, and release-production builds reject unidentified compiler wrappers and ambient Cargo profile overrides. Windows production and test-hook Rust suites passed 67/67; strict Clippy, rustfmt and Rust docs passed; the rebuilt addon/source handshake and the 46-test native-required overlay passed. An exact `git archive` of this commit built on Oxygen with Rust 1.92.0 and passed 67/67; Windows and Linux embedded the same source SHA-256 `4ecb346057a4ec73711fcaf2c8183d6c06c4987fa92cf21fdf77925f7afec76b`. TypeScript, ESLint and Vite passed. The 79-file JavaScript run passed 409 tests with three platform skips and one existing SQLite child-process test timing out under suite contention; that file then passed 5/5 alone. | This is a bounded Stage 3 foundation, not the Stage 3 exit. No N-API engine adapter, production authority, checkpoint-v3 file, SQLite pointer transaction, idle soak or per-boundary trace is claimed. Normal TypeScript startup remains unchanged. The Oxygen archive intentionally contained no `.git`, so its diagnostic addon revision was `unknown-revision`; the exact source commit is established by the local `git archive` input and matching source digest, not by that field. |
| 2026-08-10 | Stage 3 experimental coarse Node/Rust bridge | `1058acf` | Added an explicitly loaded, non-production N-API coordinator adapter with an exact source/build/contract handshake, bounded atomic probe batches, exact unsigned-64-bit identities, one weak payload-free/coalesced wake, one bounded Node drain consumer, terminal cross-language fault propagation, asynchronous stop/join, and post-stop/fault health. The normal server startup path is not connected to it. Bridge unit/native tests passed 21/21; the native-required overlay passed 52/52; release all-feature Rust tests passed 74/74 with rustfmt and strict Clippy; TypeScript, ESLint and Vite passed. An independent non-writer review found no remaining blocker. The complete JavaScript suite passed 81 files/431 tests with three skips when the unchanged SQLite child-process probe was given a ten-second per-test ceiling. | This remains below the Stage 3 exit. No production authority, checkpoint-v3, SQLite pointer publication, ten-minute idle evidence, sustained bridge load, deliberate allocator-pressure run, Linux/Oxygen bridge execution or per-boundary game trace is claimed. Under the default five-second ceiling, two full parallel runs each passed every other file but timed out that existing SQLite probe; the probe passed 5/5 alone in 1.47 seconds. |
| 2026-08-10 | Stage 3 coarse-bridge foundation evidence | Runner correction: `6d5276c`; evidence: `88952a2` | Retained real release-addon measurements from exact clean source `6d5276c94260ee0020470b43a9203150dcb71822` on the Windows development machine and the Debian `oxygen` Ryzen 7 2700 VM. Both completed the required 600-second idle interval without wake or queue growth, then reconciled every submitted, processed and completed command under bounded sustained load. Oxygen completed 1,801 batches/7,204 commands at 29.9998 batches/s and 119.9991 commands/s with command-latency p95 0.431 ms/p99 1.285 ms, load event-loop p99 11.182 ms and peak RSS 105,185,280 bytes. Windows completed 1,800 batches/7,200 commands with command-latency p95 0.203 ms/p99 0.369 ms, load event-loop p99 16.310 ms and peak RSS 79,638,528 bytes. Every recorded assertion passed; the independently checked artifact hashes are listed in `docs/todo/evidence/stage3/SHA256SUMS.txt`, whose SHA-256 is `d181af917ae539adb97097f4be020dfbe72ef76684a986d49aa2f0c48a3ab926`. | This satisfies only the Stage 3 foundation gate's ten-idle-minute coarse-bridge evidence item. It is an isolated probe path, not Rust-authoritative gameplay, checkpoint-v3/persistence-worker durability, allocator-pressure, per-boundary game tracing, or simulation throughput; the Stage 3 exit remains open. |
| 2026-08-10 | Stage 3 persistent state and reusable calculation contract | `ac815e7ffaa60d6e82f2a1c0fab799bc3c477c7e` | The Rust runtime can now own one validated authoritative-state skeleton before start; the inseparable graph bundle retains its source definition and canonical compiled layout for later checkpoint construction; state admission charges the retained source capacities and runtime wrappers. A fixed-capacity calculation workspace resolves actual snake/brain/population records, rejects stale or duplicate mappings, preserves exact `u64` identities, reuses bounded per-worker scratch, exposes disjoint partitions and refuses incomplete or identity-mismatched sealing. Real scoped 1/2/4/7-worker tests are bit-identical. The stable native tree/addon source SHA-256 was `2e5b67356b8a27ad6c397ef3f19e637d34f18aa9c78c48b23ce969f56b3b0b37`; Rust passed 87/87 with all features, strict Clippy, rustfmt and docs; focused bridge tests passed 26/26, native-required passed 57/57, and TypeScript, ESLint and Vite passed. An independent non-writer review found no remaining blocker in this slice. | This does not expose Rust authority through N-API or change normal startup. The generic calculation callback/result must remain allocation-free/fixed-size or separately account indirect storage when it gains a production caller. The complete JavaScript run twice passed 435 tests with three platform skips but the known SQLite legacy-slice subprocess exceeded its five-second timeout under full-suite contention; that file passed 5/5 alone in 1.41 seconds. Checkpoint-v3 and the persistence-worker pointer transaction remain open, so no Stage 3 exit is claimed. |
| 2026-08-11 | Stage 3 managed checkpoint-v3 codec and metadata worker | `a7e345f`, `0ecad54`, `fb0c0df` | Added the selected immutable managed-file codec with strict USTAR parsing, five bounded logical roles, per-payload raw-Float32 or shuffled-Zstandard selection, one logical hash per role/root, bit-exact restore, checked allocation limits, single-pass fsync/no-replace publication and scalar descriptors. Added the dedicated synchronous-FULL SQLite worker for descriptor metadata/current pointers only. Adversarial fixes close hidden-name/trailing-frame acceptance, undeclared codec/population workspace, oversized Zstandard windows, zero transition epochs, concurrent publication sync and valid zero-weight graphs. The corrected Windows all-feature Rust suite passed 107/107; focused persistence/format tests passed 22 with one platform symlink skip; TypeScript, targeted ESLint, rustfmt, strict Clippy and Rust docs passed. | These commits did not yet connect Rust publication to the Node metadata worker or normal authority. Retention, archive HTTP transfer, legacy conversion, startup recovery, representative continuation and Stage 3 exit remained open. |
| 2026-08-11 | Stage 3 Rust-to-SQLite checkpoint publication handoff | Implementation: `670041f`; clean-checkout harness fix: `935c887` | A feature-gated asynchronous N-API fixture now admits a real generation-boundary Rust state, publishes through the production checkpoint codec and returns only a strict scalar descriptor; no graph, population, weights, recurrent arrays or archive bytes cross N-API. The self-contained tracked harness builds separate temporary production/test-hook addons, rejects source-hash drift, proves the production addon has no hook, and ran 4/4 on exact clean commit `935c887` on Windows and Oxygen. Both builds used native source SHA-256 `b83e621f99c9979b467dce8c2a6f52c478e9272f87f63acdfdffe347aacb25fc`. The production addon hashes were Windows `9923e8c91746c37d11e0f7008a21a74388b079250171d21faa231475668d4256` and Oxygen `4d3dc02a9cd8a68bd6c3d73c384e5c143ea547669f092d35cb2a702eb4c16f15`. Tests prove final file publication precedes SQLite creation/commit, a valid descriptor atomically advances metadata/current pointer, and truncated/deleted finals leave zero metadata and no pointer. Windows all-feature Rust passed 109/109; Oxygen passed 110/110 including its symlink case; native-required passed 57/57; final JavaScript passed 451 tests with seven declared skips; TypeScript, ESLint, Vite, rustfmt, strict Clippy and Rust docs passed. Independent review found and the implementation fixed an invalid nonzero recurrent boundary fixture, silently skipped hook cases/stale-hook evidence, and a generated-loader dependency in clean checkouts. | This proves the small isolated Rust-file-to-SQLite-pointer seam only. It does not prove representative P0/P2 checkpoint continuation, next-step equivalence, normal runtime authority/swap acknowledgement, startup recovery, retention/pruning, imports/exports, broad failpoints, production latency/RSS or Stage 3 exit. Normal production startup remains unchanged. |
| 2026-08-11 | Stage 3 representative managed-checkpoint round trips | Runner: `944e486`; evidence: `6d1b274` | Exact clean commit `944e4868d1421ad1e1dc70a396f158ddc52e8ff9` ran one small, P0 and P2 boundary through the production Rust publisher, strict restore/admission and content-addressed re-publication on Windows and Oxygen. Both builds embedded native source SHA-256 `a74aca517ae664f119f24fd732b6749d8f563c011e30b79a76d5c26a33a91299`. P0 is 55 distinct 13,458-parameter genomes; P2 is 55 distinct 402,914-parameter genomes. Every full-state, source/compiled-graph, Float32 weight-bit, zero-recurrent-bit, next-RNG/allocator-probe and idempotent-root assertion passed. The P2 managed file was 66,066,432 bytes, above the old 50-MiB JSON limit. Oxygen P0 publish/restore was 17.969/14.777 ms; P2 was 442.247/384.201 ms; process high-water RSS was 273,469,440 bytes. Windows P0 was 20.924/15.538 ms and P2 was 364.219/227.683 ms. Rust passed 110/110 with all features and 107/107 in the production feature set; rustfmt, strict all-target Clippy and Rust docs passed. One independent non-writer persistence review found no blocker or P1 defect. Artifact hashes are indexed by `docs/todo/evidence/stage3/SHA256SUMS.txt`, whose SHA-256 is `bee1cdc6e8c5708d3e28df52ba33363391f5c4a9ae863de6aded60c725d7a6fb`. | The weights are deterministic source-shaped high-entropy fixtures, not owner saves or output from a completed evolved generation. “Continuation” here proves the exact generation-boundary graph/RNG/allocator/packed-weight/reset-state contract only; it does not execute sensing, inference, movement, collision, evolution or a real world step, and the artifact says `actualWorldStepExercised: false`. These are single observations, not p95 latency; Windows self-RSS is unmeasured. No browser/HTTP import-export, normal authority swap, startup recovery or Stage 3 exit is claimed. Normal production startup remains unchanged. |
| 2026-08-11 | Stage 4 scalar graph and heterogeneous-population foundation | `59215c39c7caa7bcad392446dab5822b0d5cf7b4` | Added one Rust execution plan for Input, Split, Concat, Dense, MLP, GRU, LSTM, RRU and ordered output mapping. One coarse operation now resolves each prepared brain's own packed weights and recurrent state, evaluates the due heterogeneous population with reusable scratch, stages outputs/recurrent blocks without mutating authority, and commits recurrent state only after complete finite-value and identity preflight. Tests cover current-TypeScript two-step parity for every node type, hidden-width-two recurrent indexing, 55 distinct genomes, equal-input/state weight isolation against direct evaluation, shuffled/shrinking due lists, entity-owned resurrection, reset, malformed ranges/buffers and pre-write rejection. Windows optimized Rust passed 120/120 with all features and 117/117 in the production feature set; rustfmt, strict all-target Clippy and Rust docs passed. The focused TypeScript parity suite passed 7/7, the native-required overlay passed 57/57, and the complete JavaScript suite passed 453 tests with seven declared skips; TypeScript, full ESLint and the Vite production build passed. Exact committed source then passed the 10 focused inference tests and all 121 optimized all-feature Rust tests on Oxygen's assigned eight logical CPUs of its Ryzen 7 2700 under Debian Linux 6.12.94. One independent non-writer review found no formula or implementation blocker and its two evidence gaps plus late-NaN wording gap were corrected before commit. | This is an internal scalar inference foundation, not the Stage 4 exit. It is not connected to sensing, the coordinator, fixed-step authority or normal startup; it has no SIMD selection, activation-capture interface, P0/P1/P2/P3 performance artifact, population-replacement integration or controller-output authority commit. Normal TypeScript startup remains unchanged. |
| 2026-08-11 | Stage 4 heterogeneous-inference performance checkpoint | Harness: `85f52ee`; target-host correction: `8b6dc57`; evidence: `61970bf` | Exact clean source `8b6dc57b1cd1cc026d7f6142919972c6fd5aeef4` measured P0–P3 scalar Rust, current TypeScript/JavaScript and current TypeScript/count-one-native complete-population inference on Windows and Oxygen. All paths used identical deterministic synthetic per-brain weights, observations and nonzero recurrent state; direct element-wise comparison found zero values outside `1e-4` and a maximum absolute difference below `9.69e-8`. Oxygen Rust scalar p95 was 1.07 ms P0, 5.69 ms P1, 26.45 ms P2 and 142.04 ms P3. The count-one SIMD path, despite 165/900 N-API calls, was 0.65/3.83/8.76/45.51 ms. Rust scalar target high-water RSS was 6.5/19.4/88.5/466.2 MiB versus roughly 107–111/133–138/272/1,032–1,034 MiB for the TypeScript processes. The independently reviewed harness now rejects substituted report roles, stale/debug/unsupported builds and false target provenance; its initial Oxygen attempt correctly failed because non-interactive SSH lacked `HOSTNAME`, and `8b6dc57` changed the check to read the OS hostname before any target result was accepted. The 33 indexed artifacts are covered by `docs/todo/evidence/stage4/SHA256SUMS.txt`, SHA-256 `2a6aa0721f3630d76cb640fa930c38b6b1eb07dad06c7df4df6f4ed2eed7d0c6`. | This is source-shaped synthetic inference-only evidence, not actual evolved genomes, delivered sensors, sensing, physics, coordinator, browser/LAN, trainer, fixed-step or sustained-generation evidence. Scalar P2 misses the 16.67-ms interim step budget on Oxygen, while the existing SIMD arithmetic shows sufficient inference-only headroom; Stage 4 therefore remains open and proceeds to scalar-parity-gated SIMD inside the coarse Rust operation before sensing. P3 remains capacity-only. Normal production startup remains unchanged. |
| 2026-08-11 | Stage 4 coarse runtime-selected SIMD inference | Implementation: `69997a0`; evidence: `c9439a0` | The immutable Rust graph plan now selects the existing SSE2 dot arithmetic once after runtime CPU detection while retaining an explicit scalar reference; safe equal-length slices are the only new route into the existing narrow unsafe kernels, and the operation remains coarse with zero N-API crossings inside the population, graph or layer loops. P0/P2 whole-population scalar/SSE2 tests use 55 distinct weights, observations and nonzero recurrent states; 32-step node coverage includes Split, Concat, Dense, MLP, GRU, LSTM and RRU. Exact clean source `69997a0ad7b605f47d8f83860378ace02dd4c307` then measured Rust SSE2, Rust scalar, current TypeScript/JavaScript and current count-one native P0–P3 on Windows and Oxygen with native source SHA-256 `6fbe621157cfd737d0bd941ea95331a7f2eddafb1b36412e9dd843ed08c728d4`. Oxygen SSE2 p95 was 0.44/2.45/8.45/45.07 ms for P0/P1/P2/P3 versus scalar 0.78/4.18/23.10/116.59 ms; P2 therefore clears the inference-only 16.67-ms interim budget. Every direct raw comparison had zero values outside `1e-4`; maximum absolute difference was below `9.69e-8`. Oxygen SSE2 high-water RSS was 6.4/19.5/88.5/466.2 MiB. The independently reviewed comparator rejects role substitution, stale/debug/unsupported builds, mismatched native source/target, dirty or different current-runtime commits, pass-count mismatch and normalized-host mismatch. Windows all-feature/production Rust passed 125/125 and 118/118; focused Oxygen inference passed 15/15; focused TypeScript evidence passed 6/6; TypeScript, targeted ESLint, rustfmt, strict all-target Clippy and Rust docs passed. The 41 indexed artifacts are under `docs/todo/evidence/stage4/simd-v1/`; its manifest SHA-256 is `a130cdcd2327b8e3b74aadc06def9677358c128a77cf2fedbe252c5840b5b055`. | The retained fixture is still source-shaped synthetic inference-only evidence, not an actual fresh/evolved population using delivered sensor observations. Sensing/spatial queries, activation capture, population replacement integration, physics, frames, Node coordination, LAN browser, RL trainer, complete fixed steps, bounded parallel workers and sustained generations remain open. P3 remains capacity-only; Stage 4 and normal Rust-authoritative production cutover remain open. |
| 2026-08-13 | Stage 4 corrected sensor-v3 and spatial-index checkpoint | Sensing: `58a5ae8`; harness: `5a4d093`; optimization: `07b0ae7`; evidence: `e03922d` | Rust now computes all sensor-v3 scalar and binned values over complete deterministic body/pellet indexes, keeps collision-complete queries separate from bounded sensor queries, conservatively saturates body hazards on sensor-cap hits, returns explicit delivery markers, and retains reusable scratch. The optimized path delays bounded heaps until capacity, uses a bounded dense-cell lookup with sorted sparse fallback, compacts pellet candidates and skips irrelevant head square roots. Exact source `07b0ae7c8967c484db18d385a3f8f6ee0a4df97a` embeds native source SHA-256 `f740df61ad4ca46703afcf23e898ec8e4ba63b614c805bc60620cf9a81b05856`. Oxygen optimized sensing p95 was 4.14/23.75/4.87/103.47/9.43 ms for P0/P1/P2/dense-body/dense-pellet, versus retained pre-optimization 7.43/36.87/7.03/113.18/12.19 ms. All before/after and Windows/Oxygen observation digests and query-work counts match per scenario; warmed sensing recorded zero allocations. Windows passed 151 all-feature/all-target Rust tests plus the allocation test, strict Clippy, rustfmt and the retained TypeScript sensor-vector test. One independent non-writer review found no blocker/P1; its sparse-coordinate overflow fallback, end-to-end fallback coverage and diagnostic-wording findings were corrected before the optimization commit. The 16-artifact manifest is `docs/todo/evidence/stage4/sensing-v1/SHA256SUMS.txt`, SHA-256 `b6d6adc3fdec7b92c1a3d185ee261797a63fbd771817f9e7fb847dc1758cfecf`. | P1 remains above the 16.67-ms interim single-step budget on Oxygen, so the performance correction gate and Stage 4 exit remain open. Dense-body intentionally reaches both sensor-only caps for all 310 snakes while retaining all 243,617 body-index entries; it is capacity/stress evidence, not a supported complete-step timing claim. These are deterministic source-shaped sensing-only fixtures, not owner saves, actual evolved worlds, combined inference, physics, frames, Node/browser/trainer integration or sustained generations. |
| 2026-08-13 | Stage 4 joined sensing/inference control boundary | Integration: `f8452f3`; harness/allocation fix: `9c38b48`; evidence: `91ddf25` | One coarse Rust operation now canonicalizes due brains, constructs corrected sensor-v3 observations, resolves each brain's distinct packed weights and recurrent state, evaluates the complete graph and atomically preflights then commits all observation-delivery/recurrent state. Explicit focused capture includes MLP hidden/output ranges without changing the normal path; replacement tests prove zeroed new-epoch state and stale-handle rejection. The benchmark's process allocator exposed 440 eagerly constructed success-path error strings per P0 evaluation; `9c38b48` removes them and adds a zero-allocation regression. Exact clean commit `9c38b487d07ce3ec88794a5cd793c2cf7624cc04`, native source SHA-256 `1ffbbeabeb6d133402b34e99f21615b26c216f184fe43bb2ba453e0d1282a772`, measured combined P0/P1/P2/P3 p95 of 4.65/28.71/13.82/71.97 ms on Oxygen and 2.68/13.42/7.70/40.51 ms on Windows. Warmed sensing/inference and commit made zero allocations; index construction made seven or eight. All cross-platform observation digests, query-work counts and capacities match. One independent non-writer reviewer found no blocker/P1; its all-slot uniqueness, visible delivery-change proof and accumulator-label findings were corrected before measurement. Artifacts are indexed by `docs/todo/evidence/stage4/control-v1/SHA256SUMS.txt`, SHA-256 `16611d8515cdc393878202b55661cdadb13ba953112494a43c6d23c472eeac21`. | P1 remains above the 16.67-ms interim budget, so the approved profile/correct and bounded Rust calculation-worker work remains mandatory. This is a deterministic source-shaped, single-worker control boundary, not owner saves/evolved runs, controller selection, movement, food, collision, frames, Node/browser/trainer integration, complete fixed steps, sustained generations or production cutover. |
| 2026-08-13 | Stage 5 source map and controller leases | Source map: `754c685`; controller transitions: `6bfcb24` | Retained the current TypeScript `World.step`/`Snake.advance`/collision/controller ordering with preserve-versus-correct classifications and current kill-credit examples. Rust controller leases now use monotonic wall time and server arrival sequence, hold the newest accepted input for exactly 500 ms, use exclusive neutral control through the configurable 30-second disconnect grace, and permit one explicit post-grace neural takeover without mixing inputs. Proposal commits revalidate the complete lease/action and snake-control snapshots before any write; same-millisecond replacement, clock regression, stale connection/lease, death/brain changes and recurrent-reset boundaries have focused regressions. One independent non-writer review found three authority-transition P1 defects in the initial implementation; action identity/arrival sequencing, persistent wall-clock guards and snake snapshot validation resolved them before commit. | This is internal Rust controller logic, not yet the complete fixed-step coordinator, browser/LAN path or RL-client integration. The future coordinator must retain newest-action draining before every eligible step and structurally prevent stale prepared work from committing. Stage 5 remains open. |
| 2026-08-13 | Stage 5 deterministic movement/body staging | `1d151e3` | Added reusable stable-ID movement staging for boost burn, speed/turn integration, body shrink/follow/growth, radius, wall death and deterministic pre-jitter boost-pellet requests without mutating authority. A retained executable TypeScript extractor reproduces ordinary and boost fixtures exactly and records every deterministic RNG draw. Rust tests cover TypeScript numeric parity, ordinary shrink without boost pellets, reversed source order, checked body/pellet capacity, malformed/non-finite rejection, warm capacity reuse and an admitted wall-death state. The Rust representation normalizes the dead body's first point to its final scalar position because TypeScript leaves those internal fields inconsistent; the observable wall death, body length, lack of corpse drops and skipped follow/grow/radius work remain unchanged. Release all-feature/all-target and no-default-feature Rust suites, strict Clippy, rustfmt, TypeScript, targeted ESLint, exact generated/retained JSON comparison and diff checks passed. One independent non-writer review found the wall-state admission P1 plus reproducibility and finite-overflow P2 gaps; all were corrected and re-reviewed with no remaining blocker/P1/P2. | Food claims and post-food growth, RNG realization, collision/death/drop transaction, authoritative commit key/snapshot, spawn, scheduler integration, complete-step benchmarks and production cutover remain open. `MovementConfig::typescript_defaults` is a comparison fixture; production projection must enforce approved setting ranges before wiring live/imported settings. |
| 2026-08-13 | Stage 5 deterministic food claims and body finalization | `a620c85` | Added one immutable movement-to-food phase that evaluates moved live heads against the complete pellet index, resolves contested pellets by nearest eligible head with stable-snake-ID ties, applies claims and survivors in stable pellet-ID order, and only then finalizes ordinary body growth/shrink and radius. It carries the exact borrowed source world, stable movement proposals and pre-jitter boost-drop requests forward without mutating authority; checked body/pellet limits reject the whole preparation, while a consumed pellet can free admitted storage for a later boost drop. The executable TypeScript artifact now includes a food/growth fixture. Tests prove exact movement/food numeric parity, same-substep food before ordinary shrink, pre-finalization-radius eligibility, exact-distance stable-ID ties, reversed container invariance, dead-snake exclusion, source identity, non-finite/capacity rejection and warm capacity reuse. Windows release all-feature/all-target Rust passed 193 tests plus its two binary tests; the no-default-feature library passed 181; strict Clippy, rustfmt, TypeScript, full ESLint, Vite, exact generated/retained fixture comparison and diff checks passed. One independent non-writer review found no blocker/P1 and its two owner-rule P2 test gaps were added and re-reviewed with none remaining. | Boost-drop RNG realization, ambient spawning, collision/death/kill/drop resolution, the complete authoritative transaction/epoch revalidation, spawn, scheduling, complete-step benchmarks and production cutover remain open. Warm capacity stability is proved here, but a zero-allocation food claim awaits allocator-instrumented complete-step evidence. |
| 2026-08-13 | Stage 5 immutable swept collision detection | `b2d9648` | Added a complete checked broad phase over every cell touched by swept body bounds and a continuous relative-motion narrow phase for heads and moving segments. One immutable source-alive snapshot emits stable head/head, head/body, death and body-owner kill proposals without authority writes; simultaneous heads both die with no mutual credit, contested body owners use earliest contact then stable owner/segment order, and capacity or contact-search exhaustion rejects rather than truncating or guessing. Post-movement geometry/radii are retained separately from final post-food state so boost-removed tails stay absent, ordinary-shrink tails sweep until the final boundary, growth appears only at the final boundary, and radius growth/shrink takes effect at that boundary. Source-alive wall deaths remain obstacles for the same snapshot. Focused tests cover continuous crossings, wall/body/head combinations, killed-owner obstacles, array-order invariance, stable tie rules, new/removed tails, temporal radii, complete-capacity rejection, the retained search-exhaustion counterexample and warm capacity reuse. Windows all-feature/all-target Rust passed 220 tests plus its binary targets; the no-default-feature library passed 208; strict Clippy, rustfmt and diff checks passed. One independent non-writer review found wall-snapshot, search-exhaustion, shrink-geometry and temporal-radius P1 defects plus evidence/test gaps; all were corrected and the final 27-test collision slice was re-reviewed with no remaining blocker/P1/P2. | Collision results are still proposals. Corpse/boost pellet RNG realization, kill-score/death application, complete operation/config epoch revalidation, one all-or-nothing authoritative physics commit, spawn, fixed-step coordination, allocator-instrumented complete-step evidence and production cutover remain open. |
| 2026-08-13 | Stage 5 deterministic gameplay-effect realization | `14cf8db` | Added a post-collision staging phase that preflights the complete retained/boost/corpse pellet count, reserves one contiguous internal-ID range, realizes boost requests before stable-snake-ID-ordered deaths, and emits large then small corpse pellets with the current TypeScript count/value/body-index/jitter formulas. Each effect consumes only its owner's versioned gameplay RNG stream: evolved/resurrected use world, external uses the isolated external-controller stream, baselines use their dense per-slot stream, and evolution remains unchanged. Wall death produces no corpse draw but still stages a baseline-death event. The named TypeScript extractor and retained artifact preserve the exact length-12 pellet sequence and uniform continuation; tests prove exact formula parity, two-draw boost jitter, all stream classes, two simultaneous same-stream deaths under reversed container order, allocator/capacity rejection, post-draw failure atomicity, exact empty-boundary preservation and 24-pass capacity reuse. Windows all-feature/all-target Rust passed 231 library tests plus both enabled binary tests; no-default-feature Rust passed 219; strict all-target Clippy, rustdoc, rustfmt, TypeScript, targeted ESLint, generated-artifact equality and diff checks passed. One independent non-writer review found incorrect warmed-vector reservation arithmetic and a per-pass baseline-RNG-buffer clone; both were fixed, all requested high-impact regressions were added, and final review found no remaining blocker/P1/P2. | Effects remain non-authoritative scratch. Kill score/death application, complete operation/config epoch revalidation, one all-or-nothing physics commit, ambient spawning, baseline respawn, collision-safe spawn, fixed-step coordination, allocator-instrumented complete-step evidence and production cutover remain open. |
| 2026-08-20 | Stage 5 multi-substep physics transaction and collision-safe spawn | `2311ef2d9a2ac7fe3fe6f7d3fd89e68f7a67470f` | Added one non-authoritative working physics transaction bound to world/generation/step/population/config/operation identity and the complete movement, food, collision, effect, index, capacity, delta and kill-credit configuration. It alone drives each declared substep through retained pellet indexing, movement, food, swept collision, RNG/allocator effect realization and private stable-ID result application; no caller can stamp an independently prepared phase as current, no intermediate substep becomes authoritative, and incomplete/later-phase failure keeps the admitted source unchanged. Stable deaths, simultaneous-head no-credit, dead-body-owner credit, normal/wall baseline events, continuation across substeps, real second-substep failure and warm retained-index reuse have integrated tests. Added spawn algorithm v1, preserving the TypeScript angle/area-radius/heading draw order and iterative body construction while canonicalizing requests and live obstacles by stable identity, checking complete polylines and strict wall clearance, using bounded random attempts plus deterministic draw-free fallback, and rejecting impossible or work-budget-exceeding batches without publishing partial state or reducing population. The executable TypeScript spawn fixture and retained JSON match exactly. Windows all-feature/all-target Rust passed 258 library tests plus both enabled binary tests; no-default-feature Rust passed 246; strict all-target Clippy, rustdoc, rustfmt, TypeScript, full ESLint, Vite, fixture equality and diff checks passed. One independent non-writer review found stale phase-provenance, spatial-allocation/unused-body-index, aggregate spawn-work, wall-tangency, source-order and cross-language-fixture gaps; all were corrected and final targeted review found no remaining blocker/P1/P2. | This is not yet the one authoritative fixed-step coordinator or swap. Controller-death replacement, before-step scalar accounting, baseline respawn, ambient pellets, shared observation/control, combined body-capacity admission and domain-to-RNG-stream enforcement remain integration requirements. Spawn work defaults are provisional until P0–P3 measurement. No Oxygen complete-step performance, browser/LAN, RL trainer, generation, persistence or production-cutover gate is claimed. |
| 2026-08-20 | Stage 5 deterministic ambient-pellet staging | `53c1462fea42a4b8c67b5466d17b58e3a09f482b` | Added one unavailable-until-complete ambient proposal bound to the full fixed-step key, immutable world, world RNG, allocator continuation, fractional spawn credit, advanced generation time, fixed delta, projected config and pellet ceiling. It preserves TypeScript's fractional credit while full, target-deficit refill, angle/area-radius/acceptance draw order, eight-attempt strict-first fallback and generation-time density formulas while atomically staging exact Rust pellet IDs, RNG/allocator continuations and all generated pellets. Reusable serialized-RNG output avoids replacement allocations on the warmed uniform path. The named current-source extractor and retained artifact reproduce a six-attempt acceptance, all 24 fallback draws and a distinct generation-time/fixed-delta accumulator case. Windows all-feature/all-target Rust passed 267 tests plus both enabled binary tests; no-default-feature Rust passed 255; strict all-feature Clippy, rustdoc, rustfmt, TypeScript, targeted ESLint, exact fixture regeneration and diff checks passed. One independent non-writer review found incomplete source provenance plus serialized-RNG allocation and fixture-coverage gaps; complete input revalidation, retained string storage and both missing fixtures resolved them, and final review found no remaining blocker/P1/P2. | This remains non-authoritative scratch pending the complete fixed-step coordinator and one authority swap. Before-step scalar accounting, baseline respawn, unified controller sampling, complete body/pellet capacity admission and production settings projection remain open. Draft 4 does not settle whether an impossible collision-safe mid-generation baseline respawn rejects the entire step or leaves the slot dead for a later retry; that user-visible rule requires review before dependent coordinator behavior is finalized. No complete-step performance, Oxygen, browser/LAN, RL trainer, generation, persistence or production-cutover gate is claimed. |
| 2026-08-20 | Stage 5 once-per-step scalar accounting | `64f1a899e3d54a198c27d1dea06066c37ed3d9f6` | Added reusable stable-ID accounting for generation elapsed time plus every live snake's age and configured survival points before ambient generation and the shared observation boundary. The result retains and revalidates the complete fixed-step key, immutable source world, elapsed time, fixed delta, reward setting and snake ceiling; all derived values and target fields are preflighted before a matching coordinator-owned working copy can change. Existing dead snakes remain unchanged, admitted live snakes must already own a body, and the same validated formula is exposed for later same-boundary baseline newborns. Tests prove current TypeScript numeric parity, container-order independence, malformed/overflow rejection, complete stale-input rejection, atomic no-write failure, prevention of double application and 24-pass retained-capacity reuse. Windows all-feature/all-target Rust passed 273 tests plus both enabled binary tests; no-default-feature Rust passed 261; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent non-writer review found no blocker/P1; its stale elapsed/delta/config/capacity and double-application P2 coverage gap was corrected before commit. | This is still non-authoritative staging. The future coordinator must run it first exactly once, apply the same increment to successfully respawned baseline newborns, use the updated points at the unified observation boundary, initialize and later update generation-best sensor state at the approved lifecycle boundaries, and publish only after every phase succeeds. Baseline respawn, controller joining, ambient/accounting/physics composition, complete-step performance, Oxygen, browser/LAN, RL trainer, generation, persistence and production cutover remain open. |
| 2026-08-20 | Stage 5 baseline lifecycle and respawn timing | `7fe57c24b19e978530220d1b98853e70fcf766c0` | Added generation-scoped dense baseline-slot lifecycle state plus reusable pre-control timer staging. Notified deaths start the full configured delay and first subtract on the next fixed step; missed notifications start the full delay without same-boundary subtraction; lowering the live delay caps before subtraction, raising it does not extend a wait, and exact expiry emits stable due slots while neutralizing dead actions. Physics notifications enter only through a private-field view produced by a complete `PreparedPhysicsStep`; preparation and application revalidate the full step key, exact lifecycle source, post-physics slot/entity mapping, config and untouched working copy, including a same-slot/same-ID stale-world-epoch regression. `SnakeState.baseline_strategy` is the sole canonical strategy value rather than an unchecked lifecycle duplicate. Initialization rejects missing/out-of-range slots, zero or duplicate IDs, missing strategy and reduced populations; timer output is invariant to world container order and retains all three owned vector capacities. Windows all-feature/all-target Rust passed 286 tests plus both enabled binary tests; no-default-feature Rust passed 274; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent non-writer review found stale raw death/respawn provenance and duplicate-strategy P1 defects plus initialization/order P2 gaps; the raw mutators and duplicate strategy were removed, keyed death staging and missing regressions were added, and final review found no remaining blocker/P1/P2. | This is lifecycle/timer staging, not collision-safe baseline respawn. The raw successful-respawn mutator was deliberately removed; RNG/ID/body construction, world-owned strategy reset, lifecycle reset, body-capacity admission and the one authoritative swap must be joined by the later keyed coordinator. Draft 4 does not choose whether an impossible mid-generation placement rejects the complete step or retains the dead slot for a later retry, so no dependent failure behavior is implemented. Full baseline strategy execution, unified observation/control, complete-step performance, Oxygen, browser/LAN, RL trainer, generation, persistence and production cutover remain open. |
| 2026-08-20 | Stage 5 baseline strategy and action evaluation | `95b33a4b5c3cd51f55dbdb9dc56e69eaba763608` | Added a pure reusable evaluator for one live baseline snake from an already-built corrected sensor-v3 observation. It preserves the current small/medium/large thresholds, durable roam/seek/avoid/boost timer transitions, exact per-slot uniform draw boundaries, bin scoring and safety vetoes, medium cutoff/encircle and action-only hunter override, large crowd centroid, and size-specific boost rules. Other snakes are canonicalized by stable internal ID, correcting TypeScript container-order ties and centroid accumulation. The named TypeScript extractor invokes the current public `BaselineBotManager.update`; its six retained scenarios reproduce semantically exactly and are formula/RNG evidence rather than performance evidence. Validation rejects malformed observations, slot/snake/RNG mismatches, impossible strategy/timer or wander state, and finite inputs whose distance or centroid arithmetic overflows. Windows all-feature/all-target Rust passed 296 library tests plus both enabled binary tests; no-default-feature Rust passed 284; strict all-target Clippy, rustdoc, rustfmt, TypeScript, targeted ESLint, generated-artifact equality and diff checks passed. One independent non-writer review found two P2 validation/coverage gaps around impossible durable state, derived overflow and life-stage edges; all were corrected and the final recheck found no remaining blocker/P1/P2. | This is pure calculation, not an authoritative control commit. It deliberately does not sample sensors, update observation-delivery markers or expose an apply method. The later keyed coordinator must evaluate all live baseline slots from the one shared pre-movement boundary, join strategy/action/RNG/delivery state atomically, retain latest-value external control and complete neural population evaluation, and publish only a complete fixed step. Collision-safe baseline respawn and its unresolved placement-failure rule remain outside this slice. No complete-step performance, Oxygen, browser/LAN, RL trainer, generation, persistence or production-cutover gate is claimed. |
| 2026-08-20 | Stage 5 joined fixed-step prefix | `fd3c721` | Added the first composed pre-control boundary: one complete key and immutable world/RNG/allocator/lifecycle source now drive once-per-step accounting, ambient pellet generation and non-due baseline-timer staging into one reusable non-authoritative working state. Generation time advances before ambient generation, accounting applies once, generated pellets and exact continuations join only after complete validation, and every source/config/scalar input remains available for later revalidation. A due baseline respawn explicitly withholds the result instead of selecting an unapproved placement-failure rule. Successful integration covers live accounting, ambient RNG/ID continuation and a dead baseline timer decrement with its stream untouched. Reusable controller and RNG text storage includes logical cached-Gaussian `Some`/`None` transitions through idle and pellet-generating ambient paths. Windows all-feature/all-target Rust passed 302 library tests plus both enabled binary tests; no-default-feature Rust passed 290; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent non-writer review found the missing successful baseline continuation proof and two layers of Gaussian-spare capacity loss; both were corrected, and final review found no remaining blocker/P1/P2. | This is not a complete fixed-step coordinator and performs no authority swap. Collision-safe baseline respawn/newborn accounting, shared sensor construction, baseline/external/neural control joining, controller replacement, physics, generation transition, frames, complete-step allocation/performance evidence, Oxygen, browser/LAN, RL trainer, persistence integration and production cutover remain open. The unresolved mid-generation placement-failure behavior is still reserved for owner review before dependent behavior is finalized. |
| 2026-08-20 | Stage 5 explicit neural-takeover recurrent reset | `54a61bd6b62ca14f4c98917beca89c790ce1455c` | The existing coarse heterogeneous Rust control operation now accepts a canonical set of due brain handles whose evaluation starts from an exact retained zero recurrent block. Every unmarked brain keeps its own recurrent state and distinct genome weights; source world/brain state remains unchanged until the existing all-item commit. Focused activation capture follows the same reset choice as the real control output. Duplicate, out-of-order, stale and non-due reset handles reject the complete batch. Windows all-feature/all-target Rust passed 304 library tests plus both enabled benchmark-binary tests; no-default-feature Rust passed 292; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent non-writer review found a focused-visualization mismatch and missing invalid-reset regressions; both were corrected and final review found no remaining blocker/P1/P2. | This is the neural primitive for takeover, not the controller join or authority swap. The later coordinator must derive each reset only from a validated explicit lease transition, apply it on that same boundary exactly once, and atomically join lease, chosen control, observation delivery and recurrent publication. Shared baseline/external/neural sampling, browser/RL event delivery, physics and production cutover remain open. |
| 2026-08-20 | Stage 5 joined controller-selection boundary | `33207e5fe6367d5f3a585ccbf372bf2984ed5f1c` | Added one reusable pre-movement control phase over the joined fixed-step prefix and one retained complete body/pellet index pair. Live snakes are visited by stable internal ID and receive exactly one baseline, connected/held/neutral external, ordinary neural, or explicit takeover source. Baseline, connected player/RL, and due differently weighted neural observations all read the same immutable indexed world; external observations remain packed Float32 values with delivery markers pending matching Node acceptance. Disconnected grace runs no brain, while exact expiry force-evaluates only the validated takeover brain from retained zero recurrent state. A versioned finite pending-first-action cadence value preserves TypeScript's immediate first neural action across every admitted live `controlDt` change without another checkpoint field. Tests prove six mixed shuffled controller classes, neutral grace with a valid unused brain, exact takeover reset isolation, initial-action cadence after an interval increase, packed external delivery semantics, failure atomicity, world/lease/brain storage-order independence and 24-pass reported-capacity reuse. Windows all-feature/all-target Rust passed 311 library tests plus both enabled benchmark-binary tests; no-default-feature Rust passed 299; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent non-writer review found missing joined-reset proof and a first-action/live-interval edge; both were corrected and final targeted review found no remaining blocker/P1/P2. | This phase deliberately exposes no authority-writing operation. The later complete coordinator must revalidate and atomically publish lease transitions, selected snake controls/cadence, internal baseline/neural observation markers, baseline strategy/RNG/lifecycle state, neural recurrent state and only accepted external delivery markers before physics. Due baseline respawn/newborn accounting, Node send-result integration, physics/fixed-step authority swap, generation, frames, complete-step allocation/performance evidence, Oxygen, browser/LAN, RL trainer, persistence and production cutover remain open. |
| 2026-08-24 | Stage 5 authoritative fixed-step continuation ownership | `92700bf` | Added one Rust-owned generation-scoped continuation beside world, RNG, allocators, brains and scheduler for ambient pellet credit, baseline lifecycle and sensor-generation best. Running-state admission now rejects stale sensor best values, missing or mismatched baseline identity, incoherent baseline strategy/timer/wander or alive/dead timer state, and disagreeing live baseline action copies. Retained baseline capacity is charged to the authoritative memory estimate. Checkpoint-v3 bytes remain unchanged because ordinary exact saves restore the required zero/empty pre-spawn continuation. Windows all-feature Rust passed 313 library tests plus the enabled binary and documentation tests. No-default Rust passed 301 tests before the final positive test-only addition; that final authoritative-state regression then passed under all features. Strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent read-only review found two P1 admission defects and one P2 initializer defect; all were corrected, and a positive dead-baseline exception regression was added. | This closes continuation-state ownership only; no authority swap exists yet. Baseline respawn, complete control publication, physics composition, generation, frames, Node/browser/RL integration and production cutover remain open. The owner-visible impossible mid-generation baseline-respawn rule remains unresolved and has not been selected silently. |
| 2026-08-24 | Stage 5 complete internal control commit | `32f4d0611836f7c36758e64bfac125fbfa78c666` | Added one reusable non-authoritative control commit that joins the fixed-step prefix, wall-time lease transitions, baseline strategy/action/RNG and internal delivery state, due heterogeneous neural recurrent state and internal delivery markers, and exactly one selected control per live snake. External observations remain packed and their delivery markers stay pending until matching Node acceptance. Initial neural takeover clears expired external input once; later takeover-held boundaries preserve the neural action. Immutable non-population weights are reused only within matching world/population epochs and matching per-record handle, owner and shape, while recurrent state is refreshed every boundary. Windows all-feature Rust passed 317 library tests, both enabled benchmark-binary tests and doctests; no-default-feature Rust passed 305; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One independent read-only reviewer performed two bounded checks (authority/control correctness and weight-cache identity); no blocker/P1/P2 remained, no files were changed, and no review turn was blocked or wasted. | The result is a physics-ready working boundary, not the authoritative swap. Current-authority key revalidation, complete physics, controlled-death replacement, baseline respawn, generation decisions, external send-result delivery, frames, Node/browser/RL integration and production cutover remain open. The later coordinator must preserve the documented world/population epoch and brain-handle invariants. The owner-visible impossible mid-generation baseline-respawn rule remains unresolved and has not been selected silently. |
| 2026-08-24 | Stage 5 complete post-control world-step staging | `36a457815662de5fcb6db0f538968a05ee5c2fc7` | Added `engine::world_step` version 1, which accepts only the complete internal control boundary and drives all declared movement/food/collision/effect substeps before exposing one non-authoritative result. It retains the exact full `WorldStepConfig`, post-control brains and packed external observations; carries controller leases unchanged; applies keyed post-physics baseline deaths with the full delay in the same working step; and updates generation-best sensor state from the complete post-physics world. Lease and serialized-RNG copies use checked reusable outer, per-baseline and text storage. Invalid phase config, excessive/incorrect substeps and every lease-target death fail closed without source world/RNG/allocator/lifecycle/brain writes. Windows all-feature Rust passed 321 library tests, both enabled benchmark-binary tests and doctests; no-default-feature Rust passed 309 plus doctests; strict all-target Clippy, rustdoc, rustfmt and diff checks passed. One existing independent reviewer was reused for two narrow read-only checks. It found one P2 full-config provenance gap; retaining/exposing the exact config fixed it, and the final recheck found no blocker/P1/P2. The reviewer changed no files; one initial exact-name test filter matched zero tests and was immediately corrected, with no blocked review turn. | This remains staged state, not authority. A later coordinator must project and compare the admitted key and full config, resolve due baseline respawns and lease-target death cleanup/replacement, handle generation end and accepted external delivery, and perform the one authority swap. Until then, even a completed neural-takeover lease makes that snake's death fail closed rather than producing an invalid lease-to-dead-snake state. Complete-step allocator instrumentation/performance, Oxygen, browser/LAN, RL trainer, frames, persistence integration and production cutover remain open. The impossible mid-generation baseline-respawn outcome remains unresolved and was not selected silently. |
| 2026-08-24 | Stage 5 collision-safe due baseline respawn join | `22ddbddb3330a6636fdad7b06d9d6e8b137b3c67` | Upgraded the reusable fixed-step prefix to version 2 so every successfully due baseline slot is replaced before the one shared sensing/control boundary. Stable slot order, independent per-slot RNG continuation, fresh baseline-domain and exact frame-v1 IDs, complete-body collision-safe placement, same-step newborn accounting, lifecycle/strategy/action reset, stale pellet-owner cleanup and packed-body compaction are staged atomically. Earlier replacements are obstacles for later slots, while candidate and geometry ceilings remain aggregate across the complete due set. Tests prove successful same-boundary control sampling, impossible-placement atomicity and deterministic retry from unchanged authority, reversed-container equivalence, independent streams, aggregate-work rejection and warmed retained capacity. Windows all-feature/all-target Rust passed 325 library tests plus both enabled benchmark-binary tests; no-default-feature Rust passed 313; strict release all-target Clippy, rustdoc, rustfmt and diff checks passed. One existing independent reviewer performed the required read-only deterministic-authority review. Its first pass found a P1 per-slot work-budget reset and P2 stale evidence text; both were corrected, and the focused follow-up found no remaining blocker/P1/P2. No review turn was blocked or wasted, and the reviewer changed no files. | This remains a non-authoritative working boundary. The later authority coordinator must bind the retained body ceiling and complete config to one admitted `StateCandidate`, handle lease-target death/replacement and generation decisions, accept external deliveries, and perform one final swap. An impossible mid-generation baseline placement still exposes no prefix; mapping that failure to a user-visible retry or fault remains explicitly unresolved and was not selected silently. No complete-step performance, Oxygen, browser/LAN, RL trainer, frame, persistence-integration or production-cutover gate is claimed. |
| 2026-08-24 | Stage 5 atomic nonterminal fixed-step publication | `6df8399365313fe803d02a31f0735d032e1edc10` | Added a low-level Rust-owned publication transaction for one completed nonterminal running step. `begin_running_step` issues a key bound to world/generation/source-step/population/config/operation identity, and every newer operation invalidates an older proposal. `publish_running_step` requires exactly one fixed-step continuation, preserves immutable run/population/config identity, non-gameplay RNG streams, allocator domains and non-population weights, then swaps the staged world/RNG/allocator/brain/lifecycle/ambient/sensor/scheduler state only through a reversible boundary. Full post-swap admission and memory-ceiling checks run before success; returned errors and caught validation panics restore every scalar and buffer before control leaves the transaction. Tests cover successful all-field publication and scratch-buffer return, every stale key field, forbidden/regressing continuations, malformed-world and memory-ceiling rollback, retry after rollback, and rejection outside the running phase. Windows all-feature Rust passed 330 library tests and the complete all-target run including both enabled benchmark-binary tests; no-default-feature Rust passed 318. Strict release all-target Clippy, rustdoc, rustfmt and diff checks passed. One existing independent read-only reviewer was reused. Its first turn was interrupted while redundantly compiling and is recorded as wasted usage; the static-only retry changed no files and found no remaining blocker/P1/P2. | This is deliberately a low-level transaction, not the complete scheduler/coordinator, normal production path or cutover. The private coordinator must still project and bind the complete admitted configuration, drive the staged prefix/control/world-step chain from one authority, resolve external delivery and controlled-death replacement, decide generation end, and handle impossible baseline respawn according to a reviewed user-visible rule. Reset/New Run/import must advance world identity. Frames, Node/browser/RL integration, checkpoint publication, recovery, complete-step allocation/performance, Oxygen, LAN and production-cutover gates remain open. |
