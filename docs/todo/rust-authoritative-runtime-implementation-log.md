# Rust-authoritative runtime factual implementation log

This is the short factual log required by
`docs/todo/rust-authoritative-runtime-plan.md`. It records approvals, commits,
evidence artifacts, measured results, and deviations. It does not restate the
plan or turn an unfinished stage into a completed one.

## Approved plan

- Owner approval received: 2026-07-29.
- Exact approved revision: `2026-07-29-draft-4`.
- Git commit containing the exact reviewed plan:
  `7971ed2ddbda86891c77def31d980aedf96b4236`.
- Plan blob in that commit:
  `bad8dafd8304fd5f81c0f37eb812fba8f2adb2da`.
- Approval scope: the architecture, migration path, persistence/archive
  design, 22 recorded owner decisions, corrections, tests, evidence gates, and
  acceptance requirements in Draft 4.
- Earlier drafts `2026-07-23-draft-1`, `2026-07-28-draft-2`, and
  `2026-07-28-draft-3` were superseded and were not approved.

## Implementation entries

| Date | Stage | Commit | Evidence/result | Known issue or deviation |
|---|---|---|---|---|
| 2026-07-29 | Approval and Stage 1 start | Plan: `7971ed2ddbda86891c77def31d980aedf96b4236` | Owner explicitly approved `2026-07-29-draft-4`; working tree began Stage 1 from implementation base `46c2f634c4cf48b1c1d30b7b55e3373495773d4e`. | None. No earlier draft is approved; no Stage 1 exit gate is yet claimed. |
| 2026-07-29 | Stage 1 architecture record and Git-history evidence | `c489c7e` | Corrected active instructions/README, superseded ADR 0001 and the false recovery-plan authority, added ADR 0002, and retained exact Git commit/blob/command evidence in `docs/todo/evidence/2026-07-29-stage1-git-history.md`. The false-document, historical 10/20-tick controller, and commit `8330065` claims all reproduced from Git and remain distinct from current-source proof. | Runtime repairs and the Stage 1 exit gate remain open. |
| 2026-07-29 | Stage 1 body sensing and collision-index admission | `ac43cde2f0c4913c0b416f41f60077b33f019caf` | Replaced the obsolete body-sensor adapter with the production callback grid contract; neural, baseline, and external observation paths now pass a real-grid body fixture. Collision-grid rebuilds preflight and grow capacity, reject unsafe admission instead of truncating, clear stale references, and expose load/capacity/peak/rebuild/growth/admission/fault diagnostics through stats and health. A 200,001-segment regression retains and queries every entry. Focused tests: 43 passed; world/core/server integration: 27 passed; TypeScript and changed-file ESLint passed. | The 32-Mi-entry ceiling is a temporary TypeScript-reference implementation limit, not a Rust capacity decision. Stage 1 controller, scheduler, browser-input, and reliable-message repairs remain open. |
| 2026-07-29 | Stage 1 controller, catch-up, browser input and lifecycle priority | `cf0559722d24a9fbc907e42098c22d8aa87a0d5b` | Added configurable 500-ms wall-time input hold and 30-second disconnect grace; neutral ownership during grace; token/legacy reconnect of the same snake; boundary-only expiry; latest-value browser actions; independent screen-space pointer sending; Node service between interactive catch-up steps; startup-clock reset; one reliable JSON queue ahead of one replaceable frame; and outbound diagnostics. Real socket reclaim, suppressed-sensor browser/server control, write-failure and scheduler tests pass. | The browser exposes 30 Hz and 60 Hz candidates with a temporary 60 Hz default; Stage 2 must measure both before the initial configurable cadence is selected. Reset/New Run/import epoch invalidation remains in its later approved stage. |
| 2026-07-29 | Stage 1 known-defect correction fixtures | `729cbd85a8c332bf56b76b5180cfb0d71f7648c3` | Added explicit expected-failure correction fixtures for array-order collision bias (`COLL-002`), unsafe complete-body spawn admission (`SPAWN-001`), external-join world/evolution RNG contamination (`RNG-001`), and Float32 frame-ID aliasing (`FRAME-002`). Expanded the saturated-frame test to drain assignment, reclaim, sensors/control traffic and errors before the newest frame. | These four fixtures still fail against the temporary TypeScript behavior by design; they are requirements for the named Rust stages, not passing claims or golden masters. |
| 2026-07-29 | Stage 1 real browser event transmission | `2205ff66b5b758ba42cd8af50329da8e8bb4886b` | The imported browser entry point receives one sensor pose, then actual canvas move/press/release events continue to emit fresh steering and boost-off actions without another sensor or display frame. Together with the server/world integration fixture, the newest command reaches the controller registry and next eligible fixed step. Current full suite: 68 files/340 tests passed (including four declared expected-failure correction fixtures); TypeScript, changed-file ESLint and the production browser build passed. | The Stage 1 correctness/reference exit is met. The 30/60-Hz measurement checkbox remains open and carries into Stage 2; no Rust-engine or performance exit is claimed. |
| 2026-07-29 | Stage 2 reproducible source/behavior/runtime baseline | `9a2e1fe`, `8b2b216`, `6949ae3` | Added named P0–P4 fixtures and clean-source runners for current database, codec, behavior, graph, direct runtime and canonical Node-worker paths. Retained the TypeScript source-to-fixture map, fixed-seed cross-cutting behavior artifact, current kill-credit examples, development-machine P0–P4 runtime artifacts, environment identity and derived native crossing counts. | These are Windows Ryzen 7 5800X development results. Target Debian output, real LAN/browser/trainer runs, sustained dense/soak fixtures and Stage 2 exit remain open. |
| 2026-07-29 | Stage 2 codec and narrow SQLite evidence | `341c191`, `b69b2c5`, `5b09545`; corrected runner: `1e36cd7` | Measured only representative compressed byte-volume SQLite operations rather than building a competing checkpoint system. Retained raw/decimal-JSON/gzip/plain-Zstandard comparison/byte-shuffled-Zstandard/per-genome results for fresh and evolved-like P0–P3 weight fixtures with bit-exact decode. Codec evidence version 2 enforces the approved archive-v1 choice between raw packed and shuffled Zstandard only. | The initial runner incorrectly allowed plain Zstandard to win its labelled adaptive choice for evolved P0. That label/selection was corrected before production archive implementation or retention measurement. The data still covers numeric weight payloads, not a complete managed checkpoint container; target-VM and full-checkpoint measurements remain open. |
| 2026-07-29 | Stage 2 database inventory and accidental growth observation | `9669b6b`, `66a70ac` | Bounded the read-only inventory query, reproduced the retained 5,921,520 weight-byte/6,041,600 database-byte case, and retained an accidental disposable P1 current-server run that accumulated 128 checkpoints and grew to 2,155,540,480 bytes over 8.906 hours. Exact raw logs and database inventory identify the source and limitations. | The accidental run is not the controlled P8 soak, target-VM evidence, or selected managed-file design. No owner database was written. |
| 2026-07-29 | Stage 2 current browser persistence failure | `ffb5148`, `ec1cc70`, `f25bf8f` | A finite fixture host and the in-app browser reproduced the current P1 export/import path. A population with 16,149,600 packed weight bytes produced 81,293,145 bytes of decimal JSON; export peaked at 256,485,515 combined reported heap/backing bytes. Import failed at the generic request limit after 53,018,624 uploaded bytes. Raw traces, source/build identities and exact limitations are retained. | The initial long browser-tool pause was owner-approval wait for monitoring the loopback address/port, not application latency; it is excluded from every reported timing. Import parse-time peak was unobservable while the browser action was busy and is not claimed. |
| 2026-07-29 | Stage 2 current real-server external-control baseline | Runner: `24dca58`; evidence: `98b4ea4` | Retained clean-source P0/P1 30/60-Hz runs through the real current server with simultaneous Protocol 2 periodic UI control, observation-driven bot control and complete frame-v1 viewing. P0 held about 1x; P1 managed only 0.626–0.637x, dropped 6.9–7.03 simulated seconds, and delivered five frames in each fifteen-second window. | Loopback Node clients are not the real browser renderer, owner trainer, another LAN device or Debian VM. The runs do not yet select 30 versus 60 Hz. Tool permission waits are outside runner-monotonic measurement. |
| 2026-07-29 | Stage 2 accelerated retention and growth fixture | Runner/hardening: `1e36cd7`, `e489b3c`; evidence: `6205144` | Retained `retention-p8-p0-480.json` with SHA-256 `72fd0d7fbdb28a105305970210e93d01c78a7869952556e049e26cf4e40347c5`. The size-matched P0 fixture represents 480 generation checkpoints and preserves all 480 compact history and Hall-of-Fame metadata rows while pruning to 22 automatic checkpoints (50,079,788 bytes) and 50 Hall-of-Fame weight files (2,480,183 bytes). All file/metadata/current-pointer accounting assertions passed. Indexed P1–P3 projections show the 4-GiB cap, rather than count alone, controls P3. | The payload files are not checkpoint-v3 USTAR archives, are not importable or fsynced, and do not prove publication durability, restore, archive latency or target-VM performance. P1–P3 are arithmetic from retained codec artifacts, not materialized retention runs; pinned and exported files remain outside the automatic cap. |
| 2026-07-29 | Stage 2 managed-checkpoint write-validation comparison | Runner: `ac905db`; evidence: `6c3ec68` | Retained clean-source P0/P2 artifacts with SHA-256 `efa57db87552c61452ebb48240d49c562c50b33ecb254a4d4a5a2cfd40bb7e96` and `59bbd3013ea85bb65b0894b24633305f2293b115ede99f9047ad1d3f2055caed`. Seven trials of each single-pass/checksum/light-scan/full-decode choice and a strict corruption matrix select one-pass hash/count, codec/container completion, file fsync, final-length check, atomic rename and Debian directory fsync for ordinary automatic checkpoints. Frame checksum and a payload-blind second scan remain off; startup/import/restore validates strictly, and future manual export/pin creation fully decodes before success. Focused format tests passed 8/8; TypeScript, targeted ESLint and diff checks passed. | This is a disposable Node/Windows comparison, not production Rust, restore, SQLite or Debian durability evidence. P2 single-pass p95 was 587.623 ms on the 5800X and remains unproved on the Ryzen 7 2700. Windows directory fsync returned `EPERM`. The full JavaScript suite passed 361 tests but one unrelated native-worker determinism test exceeded its timeout under suite contention; that file then passed 6/6 alone. |
| 2026-07-29 | Stage 2 persistence-format compatibility inventory | `fbb6200` | Retained a current-source and Git-history matrix for format-2 SQLite rows, historical all-parent JSON and combined-gzip rows, current/older population JSON, graph-spec JSON and historical browser-local population state. History proves the compressed format introduced by `05e0b53` was a SQLite BLOB; no tracked standalone compressed population exporter or save object was found. Existing generated current/legacy persistence suites passed 27/27. | The search does not prove that external or renamed saves do not exist. Only the workspace `data/slither.db` is inventoried; the Debian VM, Unraid storage, other disks, backups and browser profiles remain open. No compatibility reader may be narrowed or retired until those owner files are identified and copied or inspected read-only. |
| 2026-07-29 | Stage 2 dense-world load-collapse timeline | Runner: `821b95e`; evidence: `6bae744` | Retained clean-source `runtime-p4-native-dense-600.json`, SHA-256 `fb8bdeaed0a8864dde8c084e847f5be081ab48c24fcda5137737f23088b63ba6`. The fixture began with 310 live snakes, 217,000 body points and exactly 216,690 indexed segments. The first completed step took 266.05 ms and reduced the load to 52 snakes/37,329 segments; after 600 steps it had eight snakes/9,905 segments. Exact admission, zero-out-of-bounds, fault-free and complete-step assertions passed. | This is an honest Windows load-collapse curve, not sustained P4, browser/LAN, or target-VM capacity. Its 1.54x average is dominated by the much smaller world after immediate deaths; the sustained-density and Ryzen 7 2700 gates remain open. |
| 2026-07-29 | Stage 2 legacy SQLite BLOB/TEXT slice probe | Runner: `8f25214`; evidence: `6bae744` | Retained clean-source `sqlite-legacy-slice-p0.json`, SHA-256 `a55a71d808d4371ad5d69347d79692d34bf65358ec78dccb024dd08e7c80d7cc`. On `better-sqlite3` 11.10.0/SQLite 3.49.2, 39 queries apiece reconstructed 2,527,124-byte BLOB and multibyte UTF-8 TEXT fixtures exactly while returning no JavaScript Buffer larger than 65,536 bytes. Focused tests, TypeScript, targeted ESLint, diff checks and an independent evidence review passed. | Boundary-only memory samples cannot observe transient SQLite/native allocation. The artifact therefore records `boundedNativeAllocationProved: false` and `productionQueryPathAuthorized: false`; Stage 6B needs a reviewed native incremental-BLOB path or a separately reviewed strict legacy ceiling. No owner data was read. |
